name: SampleApp_$(Date:yyyyMMDD)$(Rev:r)


trigger:
- Main
pool:  
  MVCLocalPool
steps:
- task: NuGetToolInstaller@1
  inputs:
    versionSpec: 
    - task: DotNetCoreCLI@2
      inputs:
        command: 'build'
